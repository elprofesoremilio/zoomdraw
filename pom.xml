<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
             http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>es.elprofesoremilio</groupId>
    <artifactId>ZoomDraw</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <javafx.version>21.0.2</javafx.version>
        <jnativehook.version>2.2.2</jnativehook.version>
        <mainClass>es.elprofesoremilio.zoomdraw.Main</mainClass>
    </properties>

    <dependencies>
        <!-- JavaFX -->
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-fxml</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-swing</artifactId>
            <version>${javafx.version}</version>
        </dependency>

        <!--
            JNativeHook: captura global de teclado/ratón sin necesidad de foco.
            Soporta Linux (X11), Windows y macOS.
            NOTA Linux: requiere acceso a /dev/input o servidor X11 activo.
                        En Wayland puro puede no funcionar; usar XWayland.
        -->
        <dependency>
            <groupId>com.github.kwhat</groupId>
            <artifactId>jnativehook</artifactId>
            <version>${jnativehook.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Compilador Java 21 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>21</release>
                </configuration>
            </plugin>

            <!--
                Plugin JavaFX para desarrollo: mvn javafx:run
                enable-native-access elimina los WARNING de métodos restringidos
                que lanzan javafx.graphics y jnativehook al cargar sus bibliotecas
                nativas con System.load / System.loadLibrary.
            -->
            <plugin>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-maven-plugin</artifactId>
                <version>0.0.8</version>
                <configuration>
                    <mainClass>${mainClass}</mainClass>
                    <options>
                        <!-- Acceso a internos de la JVM que necesita JNativeHook -->
                        <option>--add-opens=java.base/java.lang=ALL-UNNAMED</option>
                        <option>--add-opens=java.desktop/sun.awt=ALL-UNNAMED</option>

                        <!--
                            Permite a javafx.graphics y a jnativehook cargar
                            sus librerías nativas sin WARNING ni bloqueo futuro.
                            Sin esto Java 21+ lanza:
                              "Restricted methods will be blocked in a future release"
                        -->
                        <option>--enable-native-access=javafx.graphics</option>
                        <option>--enable-native-access=com.github.kwhat.jnativehook</option>
                    </options>
                </configuration>
            </plugin>

            <!--
                Fat-JAR ejecutable: mvn package → target/zoomdraw-1.0-SNAPSHOT-fat.jar
                Uso: java -jar target/zoomdraw-1.0-SNAPSHOT-fat.jar
                     (requiere JavaFX en el module-path si no está en el JDK)
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.2</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals><goal>shade</goal></goals>
                        <configuration>
                            <shadedArtifactAttached>true</shadedArtifactAttached>
                            <shadedClassifierName>fat</shadedClassifierName>
                            <transformers>
                                <transformer implementation=
                                    "org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>${mainClass}</mainClass>
                                </transformer>
                            </transformers>
                            <!-- Excluye firmas que rompen el fat-jar -->
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
